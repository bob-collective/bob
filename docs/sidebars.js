// @ts-check

const fs = require("fs");
const contractDir = "docs/contracts/src/src/gateway";

/** @type {import('@docusaurus/plugin-content-docs').SidebarsConfig} */
const sidebars = {
  learnSidebar: [
    { type: "autogenerated", dirName: "learn" },
    // TODO: Reformat now that Gateway Strategies are in this repo
    // {
    //   type: "category",
    //   label: "Gateway Strategy Interfaces",
    //   collapsed: false,
    //   /** @type {any[]} */
    //   items:
    //     fs.existsSync(contractDir) && fs.lstatSync(contractDir).isDirectory()
    //       ? fs
    //           .readdirSync(contractDir, { withFileTypes: true })
    //           .filter((dirent) => dirent.isDirectory())
    //           .map((dirent) => ({
    //             type: "category",
    //             label: `${dirent.name
    //               .charAt(0)
    //               .toUpperCase()}${dirent.name.slice(1)}`,
    //             items: [
    //               {
    //                 type: "autogenerated",
    //                 dirName: `contracts/src/src/gateway/${dirent.name}`,
    //               },
    //             ],
    //           }))
    //       : [
    //           {
    //             type: "category",
    //             label: "Gateway Strategy Interfaces",
    //             items: ["contracts/contract_references"],
    //           },
    //         ],
    // },
  ],
};

module.exports = sidebars;
